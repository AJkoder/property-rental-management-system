import apiClient from "./client";

// No dedicated "list users" endpoint exists in the backend yet for this MVP.
// Contractors are looked up by having the manager type/select from known accounts for now.
// This file is a placeholder if we add a GET /api/users/contractors endpoint later.
